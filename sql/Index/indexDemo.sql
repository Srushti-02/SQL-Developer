-- 1. Creates an index on a table Employee.
-- 2. Create Forced Index on new Table which is not exit in Database
CREATE INDEX indexname ON Employee(first_name, last_name);

-- 3. Drop Index
DROP INDEX indexname;